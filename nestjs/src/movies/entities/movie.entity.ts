// In this file, we define the structure of the data that we will use in the application.
// This is called an entity. We will use this entity to create a database table.
// We will use this entity to create a database table.

export class Movie {
  id: number;
  title: string;
  year: number;
  genres: string[];
}
